/**
 * Legacy re-export for backward compatibility.
 * New code should import from "features/bookings/hooks".
 */
export {
  reservationKeys,
  useReservations,
  useReservationDetail,
} from "../features/bookings/hooks";
