/**
 * Auth feature — TanStack Query hooks.
 * Re-exports from the existing hooks/useAuth.js for backward compatibility.
 */
export {
  authKeys,
  useCurrentUser,
  useLogin,
  useLogout,
  useRevokeAllSessions,
} from "../../hooks/useAuth";
