/**
 * Syroce Design System (SDS) — Main Index
 *
 * Single entry point for all design system exports.
 *
 * Usage:
 *   import { DataTable, PageShell, StatusBadge } from "../design-system";
 */
export {
  DataTable,
  SortableHeader,
  selectionColumn,
  PageShell,
  FilterBar,
  StatusBadge,
  ConfirmDialog,
  Timeline,
  SdsEmptyState,
} from "./patterns";
